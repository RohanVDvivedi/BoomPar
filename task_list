OPTIMIZATION
 * create worker in executor without the executor's lock

JOB feature
 * added a function to clean up the input parameter, this is done immediately after the job is either completed or deinitialized which ever happens first
   * this will help the job to clean up the input parameter even if it does not get executed, for instance on executor shutdown immediately

EMBEDDED_REFERENCE_COUNTER OR SMART_POINTER
 * make promise reference counted
